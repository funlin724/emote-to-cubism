# 素材政策（ASSETS_POLICY）

> 一句话：**公开"怎么做的"，不公开"做出来的东西"。**

## 红线（绝对不能提交到本仓库）

| 内容 | 为什么 |
|---|---|
| 游戏图集/部件 PNG/CG/立绘 | 美术资产受版权保护，入库即侵权分发 |
| moc3 / model3.json / psd / cmo3 模型成品 | 游戏美术的衍生品 |
| 任何厂商解密密钥（硬编码或配置文件） | 可能落入"规避技术保护措施"范畴，性质比工具逻辑严重 |
| 特定游戏的角色名/条目名/截图 | 可把本仓库与具体 IP 关联，徒增下架风险 |

本仓库的技术价值在于**工具与格式文档**。素材一旦入库，仓库的性质会立刻
从"格式研究与互操作工具"变成"侵权分发"。

## 密钥处理纪律

- `tools/mzs_decrypt.py` 只接受 `--key` / 环境变量 / 本地 `mzs_key.txt`；
- `mzs_key.txt` 已被 `.gitignore` 排除；
- 提交前自查：`git grep -iE "key\s*=\s*['\"][A-Za-z0-9]{8,}"`；
- 密钥也不要出现在 issue、截图、日志、示例命令里。

## CI 资产守卫

`.github/workflows/asset-guard.yml` 在每次 push/PR 时扫描：

- 扩展名：`.psb` `.psb.m` `.moc3` `.mtn` `.psd` `.cmo3` `.can3` `.model3.json`（非示例）`.physics3.json`（非示例）；
- 4096×4096 级大图（图集特征）；
- `mzs_key.txt` / 疑似硬编码密钥模式。

命中即 fail。这是防误提交的**兜底**，不是豁免人工注意的理由。

## 模型产物的建议去向

- 产出的 moc3/模型：**留本地或私下分享**，不进公开仓库；
- 演示用途：自绘角色（CC0 或自制）走完整条管线，作为 `examples/` 的
  合法测试资产。官方 E-mote 样例数据通常只授权评估用途，
  **上传前必须逐条读 EULA**，不允许再分发的一律不进仓库。

## 自造测试数据（推荐）

格式本身不受版权保护。用自有/CC0 美术 + FreeMote `PsBuild` 或自写回器
生成一份 E-mote 结构的测试数据，100% 属于自己，可安全入库作为 fixture。
